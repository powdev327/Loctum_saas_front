import Layout from "../Layout";
import ResetPasswordSection from "../Sections/Authentication/ResetPassword";

const ResetPassword = () => {
  return (
    <Layout pageTitle="QuickLocum - Reset Password">
      <ResetPasswordSection />
    </Layout>
  );
};

export default ResetPassword;
